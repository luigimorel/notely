import React from 'react'
import { useQuery, gql } from '@apollo/client'
import {}

export const NoteFeed = () => {
    return (
        <div>
            <p>ID: {URLSearchParams.match.params.id}</p>
        </div>)
}